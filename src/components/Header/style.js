import styled from "styled-components";

export const StyledHeader = styled.header`
    max-width: 100%;
    .flexBox{
        display: flex;
        align-items: center;
        height: 90px;
    }
`